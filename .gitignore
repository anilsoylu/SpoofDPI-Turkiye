# Build çıktıları
/bin/
bin/
dist/
*.test
# OS
.DS_Store
# Yerel çalışma/dağıtım dizini (kullanıcı home'unda oluşur ama her ihtimale karşı)
.spoofdpi-tr/
# Planlar repoya girmemeli (proje opensource)
plans/
# SwiftUI menu bar build çıktıları
menubar/.build/
*.app
menubar/SpoofDPI-Türkiye.app/

# Zapret 3. parti referans/clone — repoya gitmesin (telif/lisans, kaynaktan derlenir)
reference/
zapret/
# Derlenmiş 3. parti motor binary (tpws — install.sh kaynaktan derler)
*.tpws
tpws
